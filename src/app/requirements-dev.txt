fastapi
fastapi[standard]

# ---- Dependencias de desarrollo / testing ----
pytest
pytest-asyncio          # para correr tests async
httpx                   # cliente HTTP async para tests de FastAPI
pytest-cov              # cobertura de código
pytest-mock             # mocking fácil en pytest
faker                   # generar datos falsos
factory-boy             # fábricas de objetos de prueba
respx                   # mock de requests httpx (si simulas APIs externas)
moto                    # simular servicios AWS en tests (DynamoDB, S3, etc.)